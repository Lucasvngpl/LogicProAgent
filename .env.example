# Copy this file to .env and fill in your keys
# Then: source .env (or python-dotenv loads it automatically)

# OpenAI API key (REQUIRED for voice input - speech-to-text)
# Get your key at: https://platform.openai.com/api-keys
# New accounts get $5 free credits (~1,600 minutes of transcription)
OPENAI_API_KEY=your-key-here

# Vision runs locally via Qwen2.5-VL — no API key needed!
